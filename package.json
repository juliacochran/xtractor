{
  "name": "xtractor",
  "version": "0.3.0",
  "description": "String extraction for i18n",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint --ext .js . || true",
    "test": "mocha || true",
    "ci": "eslint --ext .js . && mocha",
    "build": "rm -rf lib && mkdir lib && babel src --out-dir lib",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pinterest-web/xtractor.git"
  },
  "homepage": "https://github.com/pinterest-web/xtractor.git",
  "author": "Arthur Lee <arthur@pinterest.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-eslint": "^6.0.4",
    "babel-preset-es2015": "^6.6.0",
    "chai": "^3.5.0",
    "eslint": "^2.9.0",
    "mocha": "^2.4.5"
  },
  "files": [
    "lib"
  ],
  "dependencies": {
    "babel-traverse": "^6.8.0",
    "babylon": "^6.7.0",
    "node-glob": "^1.2.0"
  }
}
